**This is your prompt:**

---

Seven changes. I've attached the file AI_Security_Continuum_V20260426C.html — you'll need it for item 4.

1. **Default landing page:** Flip the default from the registration page to the login page. When someone visits the site without a session cookie, they see the login form first. On the login page, make "Need an account? Register" prominent — 16px, gold (#C8963E) text. On the registration page, keep "Already have an account? Log in" as it currently is.

2. **Privacy message redesign:** Replace the current single-line privacy message above the input bar with two lines, both centered. First line: "AI for HHS never stores your conversations." — 16px, regular weight (not bold), gold (#C8963E). Second line: "When you close this chat, it's gone." — 16px, bold, crimson (#B91C1C).

3. **Security link:** Remove the "Enter to send · Shift+Enter for new line." text below the input bar. Replace it with a centered "Security" link styled as a pill — 14px, gold (#C8963E) text, 1px solid gold border, border-radius 20px, padding 5px 18px. On hover, add a subtle gold background tint.

4. **Security modal:** When a user clicks the "Security" pill, open a modal overlay. The modal should dim the background (rgba(0,0,0,0.65)), display the contents of the attached file AI_Security_Continuum_V20260426C.html inside the modal, and include a prominent close button — a 44px circle in the top right corner with a white X icon, dark background (#2A3A50), and a visible border (#7B8CA3). On hover the border turns gold. Clicking the X closes the modal. Clicking anywhere on the dimmed background outside the modal also closes it. The hover interactions inside the security continuum (the flyover cards on the spectrum) must work normally inside the modal.

5. **Get in Touch modal close button:** The existing Get in Touch modal closes when clicking outside it, but it needs a visible X button for users who don't know that. Add the same close button style as the Security modal — 44px circle, top right corner, white X, dark background, visible border. Clicking it closes the modal.

6. **Header link size:** Increase "Get in touch" and "Log out" in the chat header from 13px to 15px. Keep the same gray color (#7B8CA3).

7. **Task launcher card management in admin Settings tab:** Add a "Task Launcher Cards" section to the admin Settings tab, positioned between Corpus Documents and System Prompt. Display all 8 task launcher cards in a list. Each card is editable with three fields: Title (the main text, e.g. "Break down an ACL or policy letter"), Description (the subtitle text, e.g. "Upload a document and get a plain-language summary"), and Display Order (number 1-8 controlling the position in the grid). Changes save to the database and take effect on the next page load. The chat interface reads card titles, descriptions, and order from the database instead of from hardcoded values.

---